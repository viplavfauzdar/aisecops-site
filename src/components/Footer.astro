<footer>
  <div class="footer-inner">
    <span class="footer-logo">AI<span>Sec</span>Ops.net — Built by <a href="https://medium.com/@viplav.fauzdar" style="color:#888;text-decoration:none;">Viplav Fauzdar</a></span>
    <div class="footer-links">
      <a href="/definition">Definition</a>
      <a href="/threat-model">Threat Model</a>
      <a href="/reference-architecture">Architecture</a>
      <a href="/whitepaper">Whitepaper</a>
      <a href="https://github.com/viplavfauzdar/aisecops-lab" target="_blank" rel="noopener noreferrer">GitHub</a>
      <a href="https://medium.com/@viplav.fauzdar" target="_blank" rel="noopener noreferrer">Blog</a>
      <a href="mailto:hello@aisecops.net">Contact</a>
    </div>
  </div>
</footer>
<button id="scroll-top-btn" aria-label="Scroll to top">↑</button>

<script>
  const scrollTopBtn = document.getElementById("scroll-top-btn");

  const toggleScrollTopBtn = () => {
    if (!scrollTopBtn) return;
    if (window.scrollY > 320) {
      scrollTopBtn.classList.add("is-visible");
    } else {
      scrollTopBtn.classList.remove("is-visible");
    }
  };

  if (scrollTopBtn) {
    scrollTopBtn.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
    window.addEventListener("scroll", toggleScrollTopBtn, { passive: true });
    toggleScrollTopBtn();
  }
</script>

<style>
  footer {
    background: var(--ink, #0d1117);
    color: #555;
    padding: 2.5rem 2rem;
    font-family: var(--mono, "IBM Plex Mono", monospace);
    font-size: 0.72rem;
    letter-spacing: 0.04em;
  }

  .footer-inner {
    max-width: 960px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .footer-logo {
    color: #fff;
    font-weight: 500;
  }

  .footer-logo span { color: var(--accent, #c94f1e); }

  .footer-links {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .footer-links a {
    color: #555;
    text-decoration: none;
    transition: color 0.15s;
  }

  .footer-links a:hover { color: #fff; }

  #scroll-top-btn {
    position: fixed;
    right: 1.2rem;
    bottom: 1.2rem;
    width: 42px;
    height: 42px;
    border: none;
    border-radius: 999px;
    background: var(--accent, #c94f1e);
    color: #fff;
    font-size: 1.1rem;
    line-height: 1;
    cursor: pointer;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.2);
    opacity: 0;
    transform: translateY(8px);
    pointer-events: none;
    transition: opacity 0.2s, transform 0.2s, background 0.2s;
    z-index: 120;
  }

  #scroll-top-btn:hover { background: #a83d12; }

  #scroll-top-btn.is-visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  @media (max-width: 780px) {
    .footer-inner { flex-direction: column; }
    #scroll-top-btn {
      right: 0.9rem;
      bottom: 0.9rem;
    }
  }
</style>
